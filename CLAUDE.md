# session-review-skill

Claude Code skill for reviewing session transcripts. Source of truth for skill code — deployed to `~/.claude/skills/session-review/` via `scripts/deploy.sh`.

## Deploying

`make deploy`

## Bash Command Style
Never chain commands with && or ; operators. Run them as separate bash calls instead.
